program: main.py
method: grid
name: cifar10 - squared_mean, reduction='sum' except for cls_loss(with test, epochs=300/400/500, batch_size=100, lr=0.002, theta=2)
metric:
    goal: maximize
    name: Test cls_acc

parameters:
  # input.batch_size:
  #   values: [100, 250, 500]
  # training.learning_rate:
  #   values: [0.002, 0.003, 0.004, 0.005]
  training.epochs:
    values: [300, 400, 500]
  # model.theta:
  #   values: [2.0, 5.0, 10.0]

command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}