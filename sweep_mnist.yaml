program: main.py
method: grid
name: mnist - bp网络的lr调试
metric:
    goal: maximize
    name: Test cls_acc

parameters:
  input.batch_size:
    values: [100, 200, 500]
  training.learning_rate:
    value: 1e-3
  # training.epochs:
  #   values: [100, 200]
  # model.theta:
  #   values: [2.0, 5.0, 10.0]


command:
  - ${env}
  - python
  - ${program}
  - ${args_no_hyphens}
